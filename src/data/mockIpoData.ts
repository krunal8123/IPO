// All static mock data has been removed.
// IPORadar streams all data (IPOs, GMP, Subscription, Allotment, Buybacks, Calendar)
// directly from the integrated Upstox API and live market scrapers via the backend server.

export const mockIpoList = [];
export const mockBuybacks = [];
export const mockCalendarEvents = [];
